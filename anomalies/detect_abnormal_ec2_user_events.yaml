where_condition: useridentity.arn like '%i-%' and (eventname='CreateUser' or eventname='AssumeRole' or eventname='CreateKeyPair')

description: Detect CreateUser and AssumeRole event logs of the EC2 instance user.

severity: Critical

